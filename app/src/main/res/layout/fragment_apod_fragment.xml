<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/constrainLayout"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:background="#4C7277"
    tools:context=".Fragment.apod_fragment">

    <ImageView
        android:id="@+id/imvApodActual"
        android:layout_width="410dp"
        android:layout_height="306dp"
        tools:src="@tools:sample/avatars"
        tools:ignore="MissingConstraints"
        android:contentDescription="@string/apod" />

    <EditText
        android:id="@+id/EditTextApod"
        android:layout_width="0dp"
        android:layout_height="290dp"
        android:layout_marginStart="20dp"
        android:layout_marginTop="20dp"
        android:layout_marginEnd="20dp"
        android:textColor="@color/white"
        android:backgroundTint="@android:color/transparent"
        android:focusableInTouchMode="false"
        android:scrollbars="vertical"
        android:textSize="18dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/imvApodActual" />

</androidx.constraintlayout.widget.ConstraintLayout>